/**
 * @file
 *
 * @author Generated by spec2x
 * $Rev$
 * $Date$
 */
#ifndef <% Guard %>
#define <% Guard %>

<% Includes %>

#include "bi/typelist/TypeList.hpp"
#include "bi/model/BayesNet.hpp"
#include "bi/pdf/FactoredPdf.hpp"

/**
 * Type list for s-nodes.
 */
<% STypeList %>

/**
 * Type list for d-nodes.
 */
<% DTypeList %>

/**
 * Type list for c-nodes.
 */
<% CTypeList %>

/**
 * Type list for r-nodes.
 */
<% RTypeList %>

/**
 * Type list for f-nodes.
 */
<% FTypeList %>

/**
 * Type list for o-nodes.
 */
<% OTypeList %>

/**
 * Type list for p-nodes.
 */
<% PTypeList %>

/**
 * Prior type list for s-nodes.
 */
<% SPriorList %>

/**
 * Prior type list for d-nodes.
 */
<% DPriorList %>

/**
 * Prior type list for c-nodes.
 */
<% CPriorList %>

/**
 * Prior type list for p-nodes.
 */
<% PPriorList %>

/**
 * <% Description %>
 */
class <% ClassName %> : public bi::BayesNet<
    GET_TYPELIST(<% STypeListName %>),
    GET_TYPELIST(<% DTypeListName %>),
    GET_TYPELIST(<% CTypeListName %>),
    GET_TYPELIST(<% RTypeListName %>),
    GET_TYPELIST(<% FTypeListName %>),
    GET_TYPELIST(<% OTypeListName %>),
    GET_TYPELIST(<% PTypeListName %>)> {
public:
  /**
   * Constructor.
   */
  <% ClassName %>();

  /**
   * Prior over s-nodes.
   */
  bi::FactoredPdf<GET_TYPELIST(<% SPriorListName %>)> getSPrior();

  /**
   * Prior over d-nodes.
   */
  bi::FactoredPdf<GET_TYPELIST(<% DPriorListName %>)> getDPrior();

  /**
   * Prior over c-nodes.
   */
  bi::FactoredPdf<GET_TYPELIST(<% CPriorListName %>)> getCPrior();

  /**
   * Prior over p-nodes.
   */
  bi::FactoredPdf<GET_TYPELIST(<% PPriorListName %>)> getPPrior();

  /*
   * Nodes.
   */
  <% NodeDeclarations %>

};

#endif

